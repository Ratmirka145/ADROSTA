[pytest]
pythonpath = .
testpaths = tests
norecursedirs = .git .venv work outputs
markers =
    integration: requires an explicitly supplied disposable PostgreSQL TEST_DATABASE_URL
    cdek_live: requires CDEK test credentials and internet access
